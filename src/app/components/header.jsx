let React = require('react');
let mui = require('material-ui');
let {
  AppBar,
  LeftNav,
  MenuItem,
} = mui;

let Header = React.createClass({

  render() {
    return (
      <div>
        <h5>nav</h5>
      </div>
    );
  },
});

module.exports = Header;
